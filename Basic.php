<?php

// SQL = Structured Query Language (RDBMS = Relational Database Management System)
// Data stored in the form of table (Tabular DBMS) e.g: MYSQL,Oracle, PostreQl

// NoSQL = data stored in the form of document(not tubular) e.g: MongoDB, Redis

// constraints = restriction (not null, unique, primary key, foreign key, check, default, index)

// IN = shorthand of OR --- SELECT * FROM users WHERE age = 20 OR age = 18  -->> SELECT * FROM users WHERE age IN (18, 20)
// BETWEEN = to get record between two values --- SELECT * FROM users WHERE id BETWEEN 7 AND 16; (get record from 7 to 16)

